import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    typography: {
      fontFamily1: 'Font1, sans-serif', // Replace 'Font1' with the name of your first font
      fontFamily2: 'Font2, sans-serif', // Replace 'Font2' with the name of your second font
      // Add more font families as needed
    },
  });
  
  export default theme;
  